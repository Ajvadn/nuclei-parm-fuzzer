# Binaries
/parm-fuzzer
nuclei-parm-fuzzer
nuclei-parm-fuzzer.exe

# Output folders
results/

# Go specific
# go.sum (Keeping go.sum is usually best practice, but I'll follow the user's preference if they really want it ignored. However, I'll comment it out for now as I added dependencies.)
# go.sum
